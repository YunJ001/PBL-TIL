# 📘 Today I Learned

### 1. 오늘 배운 내용
- JavaScript로 작성된 React 프로젝트를 TypeScript로 마이그레이션하는 방법
- API 응답 데이터의 구조를 타입으로 정의하고 활용하는 방법
- 공통 타입을 별도 파일로 분리해 관리하는 방법

### 2. 핵심 정리 (내 언어로)
- TypeScript는 코드 실행 전 컴파일 단계에서 타입 오류를 잡아주기 때문에, 런타임에서 발생할 수 있는 버그를 미리 방지할 수 있다.

### 3. 결과 이미지(스크린샷)
![스크린샷](./result.png)

### 4. 느낀 점
- JS에서 TS로 바꾸면 컴파일 단계에서 오류를 잡아 발생 가능한 버그를 방지할 수 있다는 점을 알게 되었습니다.