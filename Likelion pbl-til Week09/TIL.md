# 📘 Today I Learned

### 1. 오늘 배운 내용
- Supabase를 사용해 데이터베이스와 인증을 연동하는 방법
- 이메일/비밀번호 기반 회원가입 및 로그인을 구현하는 방법
- 로그인 여부에 따라 기능 접근을 제한하는 권한 분리를 구현하는 방법

### 2. 핵심 정리 (내 언어로)
- 기존에는 데이터가 브라우저 메모리에만 있어 새로고침하면 사라졌지만, Supabase DB에 저장하면 새로고침하거나 다른 기기에서 접속해도 동일한 데이터를 볼 수 있다.

### 3. 결과 이미지(스크린샷)
![로그인](./result.png)
![로그아웃](./logout.png)

### 4. 느낀 점
- 백엔드 없이 Supabase만으로 DB와 인증을 모두 처리할 수 있다는 점이 인상적이었습니다.